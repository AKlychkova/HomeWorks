public class IPhone extends Smartphone{
    public IPhone(double screenSize, int memory, String bodyMaterial, String operatingSystem) {
        super(screenSize, memory, bodyMaterial, operatingSystem);
    }
}
