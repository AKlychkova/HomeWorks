public class Samsung extends Smartphone{
    public Samsung(double screenSize, int memory, String bodyMaterial, String operatingSystem) {
        super(screenSize, memory, bodyMaterial, operatingSystem);
    }
}
